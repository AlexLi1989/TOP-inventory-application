<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <%- include("partials/nav") %>
    <main>
      <aside>
      <form action="/search" method="GET">
        <input type="hidden" name="productName" value="<%= locals.LASTPARAMS?.productName || "" %>" />
        <fieldset class="category filter">
          <legend>Category (multiple allowed) :</legend>
          <% for (let category of CATEGORIES) { %>
            <div class="category">
              <% let isChecked = false;
                if (locals.LASTPARAMS?.category) {
                  if (Array.isArray(LASTPARAMS.category)) {
                    isChecked = LASTPARAMS.category.includes(category.category_name);
                    } else {
                      isChecked = LASTPARAMS.category === (category.category_name);
                      }
                    }
              %>
              <input type="checkbox" id="<%= category.category_id%>" name="category" value="<%= category.category_name%>" <%= isChecked ? "checked" : "" %> />
              <label for="<%= category.category_id%>"><%= category.category_name%></label>
            </div>
          <% } %>
        </fieldset>
        <fieldset class="sort filter">
          <legend>Sort By :</legend>
          <div>
            <input type="radio" id="price" name="sortBy" value="price"
            <%= locals.LASTPARAMS?.sortBy === "price" ? "checked" : "" %>
            >
            <label for="price">Price</label>
          </div>
          <div>
            <input type="radio" id="alpha" name="sortBy" value="alpha"
            <%= locals.LASTPARAMS?.sortBy === "alpha" ? "checked" : "" %>
            >
            <label for="alpha">Alphabetical</label>
          </div>
        </fieldset>
        <fieldset class="order filter">
          <legend>Order By :</legend>
          <div>
            <input type="radio" id="asc" name="order" value="asc"
            <%= locals.LASTPARAMS?.order === "asc" ? "checked" : "" %>
            >
            <label for="asc">Ascending</label>
          </div>
          <div>
            <input type="radio" id="desc" name="order" value="desc"
            <%= locals.LASTPARAMS?.order === "desc" ? "checked" : "" %>
            >
            <label for="desc">Descending</label>
          </div>
        </fieldset>
        <button type="submit">Filter</button>
      </form>
      </aside>
      <div class="search-products-title">
        <h1>Search Results</h1>
        <a href="/product/new" class="add-product-btn">Add New Product</a>
      </div>
      <div class="product-cards-container">
        <% for (let product of PRODUCTS) { %>
          <div class="product-card">
            <img src="<%= product.product_image_url %>" alt="<%= product.product_name %>"/>
            <h2><%= product.product_name %></h2>
            <p>Category: <%= product.category_name %></p>
            <p>$ <%= product.price %></p>
            <p>Stock: <%= product.quantity %></p>
            <p>Description: <%= product.description %> </p>
            <a href="/product/<%= product.product_id %>">Edit</a>
          </div>
        <% } %>
      </div>
    </main>
    <%- include("partials/footer") %>
  </body>
</html>